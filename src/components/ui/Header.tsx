'use client';

import Navigation from '../layout/Navigation';

export const Header = () => {
    return (
        <header className="w-full text-pretty text-base-content" role="banner">
            <Navigation />
        </header>
    );
};
